[package]
name = "neo-devpack"
version = "0.1.0"
edition = "2021"
description = "Neo N3 development pack foundation for neo-lang"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
